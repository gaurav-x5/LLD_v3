package org.example;

/**
 * this class is going to denote both snake and ladder
 * if start > end -> this denotes it is a snake
 * & if end > start -> its a ladder */
public class Jump {
    int start;
    int end;
}
